(fromList [("andThen (andThen c c2) c3 = andThen c (andThen c2 c3)",17),("andThen (clue ks bottom) c = clue ks bottom",21),("andThen (eitherC c (reward r)) c2 = both c2 (eitherC c (reward r))",34),("andThen (gate f c) c2 = gate f (andThen c c2)",16),("andThen (reward r) (clue ks empty) = clue ks (reward r)",29),("andThen (reward r) (eitherC c empty) = eitherC c (reward r)",30),("andThen (reward r) (reward r2) = reward (r <> r2)",20),("andThen bottom c = bottom",7),("andThen c empty = c",6),("andThen empty c = c",8),("both (both c c2) c3 = both c (both c2 c3)",18),("both c (clue ks (reward r)) = andThen (clue ks (reward r)) c",33),("both c (clue ks empty) = andThen (clue ks empty) c",22),("both c (eitherC c2 empty) = andThen (eitherC c2 empty) c",23),("both c (reward r) = andThen (reward r) c",13),("both c bottom = andThen c bottom",9),("both c c2 = both c2 c",2),("both c empty = c",10),("clue (ks <> ks2) c = clue ks (clue ks2 c)",15),("clue ks (andThen (reward r) c) = andThen (reward r) (clue ks c)",32),("clue ks (andThen c (reward r)) = andThen (clue ks c) (reward r)",31),("clue mempty c = c",4),("eitherC (eitherC c c2) c3 = eitherC c (eitherC c2 c3)",19),("eitherC (reward r) (eitherC c c2) = eitherC (reward r) (both c c2)",28),("eitherC (reward r) (gate f c) = reward r",27),("eitherC c (andThen (reward r) c2) = andThen (reward r) (eitherC c c2)",35),("eitherC c bottom = c",11),("eitherC c c2 = eitherC c2 c",3),("eitherC empty (both c c2) = eitherC c (eitherC c2 empty)",25),("eitherC empty (clue ks empty) = clue ks empty",26),("eitherC empty (gate f c) = empty",24),("eitherC empty (reward r) = reward r",14),("eitherC empty empty = empty",12),("empty = reward mempty",1),("gate f bottom = bottom",5)],CodeBlock ("",["haskell","ghci"],[]) "> quickSpec $ sig <> sig_opts\n  1. empty = reward mempty\n  2. both c c2 = both c2 c\n  3. eitherC c c2 = eitherC c2 c\n  4. clue mempty c = c\n  5. gate f bottom = bottom\n  6. andThen c empty = c\n  7. andThen bottom c = bottom\n  8. andThen empty c = c\n  9. both c bottom = andThen c bottom\n 10. both c empty = c\n 11. eitherC c bottom = c\n 12. eitherC empty empty = empty\n 13. both c (reward r) = andThen (reward r) c\n 14. eitherC empty (reward r) = reward r\n 15. clue (ks <> ks2) c = clue ks (clue ks2 c)\n 16. andThen (gate f c) c2 = gate f (andThen c c2)\n 17. andThen (andThen c c2) c3 = andThen c (andThen c2 c3)\n 18. both (both c c2) c3 = both c (both c2 c3)\n 19. eitherC (eitherC c c2) c3 = eitherC c (eitherC c2 c3)\n 20. andThen (reward r) (reward r2) = reward (r <> r2)\n 21. andThen (clue ks bottom) c = clue ks bottom\n 22. both c (clue ks empty) = andThen (clue ks empty) c\n 23. both c (eitherC c2 empty) = andThen (eitherC c2 empty) c\n 24. eitherC empty (gate f c) = empty\n 25. eitherC empty (both c c2) = eitherC c (eitherC c2 empty)\n 26. eitherC empty (clue ks empty) = clue ks empty\n 27. eitherC (reward r) (gate f c) = reward r\n 28. eitherC (reward r) (eitherC c c2) = eitherC (reward r) (both c c2)\n 29. andThen (reward r) (clue ks empty) = clue ks (reward r)\n 30. andThen (reward r) (eitherC c empty) = eitherC c (reward r)\n 31. clue ks (andThen c (reward r)) = andThen (clue ks c) (reward r)\n 32. clue ks (andThen (reward r) c) = andThen (reward r) (clue ks c)\n 33. both c (clue ks (reward r)) = andThen (clue ks (reward r)) c\n 34. andThen (eitherC c (reward r)) c2 = both c2 (eitherC c (reward r))\n 35. eitherC c (andThen (reward r) c2) = andThen (reward r) (eitherC c c2)\n\n\n")